# ---- Ollama Cloud (chat / inference) ----
# Native endpoint. OpenAI-compatible base is https://ollama.com/v1
OLLAMA_BASE_URL=https://ollama.com
OLLAMA_API_KEY=replace-with-your-full-key-including-the-part-after-the-dot
OLLAMA_CHAT_MODEL=qwen3.5:cloud
# Reasoning models emit a long "thinking" block first; off = snappier replies.
OLLAMA_THINK=false

# ---- Web search fallback (when RAG finds nothing) ----
# Tavily first if a key is set, else DuckDuckGo (no key needed). Get a key at
# https://tavily.com
TAVILY_API_KEY=

# ---- Embeddings (swappable) ----
# EMBEDDING_BACKEND:
#   "local"  fastembed/ONNX, fully offline, no API cost (default; bigger image)
#   "gemini" Google embeddings API — tiny runtime, fast cold starts (serverless deploy)
#   "ollama" Ollama Cloud /api/embed (only if your tier enables it)
# Multilingual model so Korean and English queries retrieve well (locale-filtered).
# NOTE: switching backends/models changes vector dim — re-run ingest (it rebuilds).
EMBEDDING_BACKEND=local
LOCAL_EMBEDDING_MODEL=intfloat/multilingual-e5-large
OLLAMA_EMBEDDING_MODEL=embeddinggemma
# Gemini: get a free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# ---- RAG / Chroma ----
CHROMA_DIR=./chroma_db
CHROMA_COLLECTION=finguru_lessons
RAG_TOP_K=5

# ---- Content / i18n ----
# Path to the MDX lessons that get ingested (English tree; locales live under it).
CONTENT_DIR=../web/content
# Locales to ingest: "en" = content/, others = content/<locale>/.
LOCALES=en,ko

# ---- Server ----
API_HOST=0.0.0.0
API_PORT=48000
# Comma-separated origins allowed to call the API (the Next.js dev server)
CORS_ORIGINS=http://localhost:48080

KIS_APP_KEY=
KIS_APP_SECRET=//z+4a5f627NMbuSMx20hEiMevr3DpNLQc3/8XgGAQp4C4awd/KAM2O5v1MG8AY5QC85WSxajVgKsxIISy4d47qBKhkKhOsIib6Eo5jNDTm6DLGkBbOqE7fH8J2+NXf5JrlkTxAZ2Q=
KIS_ACCOUNT_NO=

KRX_API=

DART_API_KEY=

EIA_REGISTRATION_KEY=

FMP_API_KEY=